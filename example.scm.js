var a = "hello";
var b = 'ola';
var go = function(a, b) {

  if ((a > b || a == 0) && !(b == 4)) {
    print(a + b);
    return print(a * b);
    
  } else {
    return print(a - b);
    
  }
  
  //(solid:block 0 0 0 1 1 1)
  
};
var b = [1, 2, 3];
print(b[1]);
//// A define of nested lists
var c = [[1, 2], //comment
[3, 4], [5, 6]];
// A car
c.slice(1)[0];
// A map
part.entities().forEach(x => entity.delete(x));
// A map with lambda
part.entities().forEach((x) => {
  return entity.delete(x);
  });
() => {
  return x + y;
  };
part.clear();
var smi0 = smi_shell();
var body0 = part.load("BODY_1.smt")[0];
smi0.set_body(body0);
smi0.set_lop_options('standard_full_rbi');
smi0.set_default_in_offset(-0.5);
smi0.set_default_out_offset(0);
var body = smi0.get_body();
ut_expect_error('LOP_TWK_TOPOL_CHANGE', () => smi0.compute());
smi0 = false;
